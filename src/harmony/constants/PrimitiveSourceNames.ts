/**
 * Names employed by the raw music sources specific to this generator.
 *
 * Ported from
 * `as-sources/legacy-generators-harmony/ro/se/legacy/generators/harmony/constants/PrimitiveSourceNames.as`.
 *
 * Per project convention (see `utils/constants/CommonStrings.ts`), the
 * original AS3 "constants class" becomes a single frozen object.
 */
export const PrimitiveSourceNames = {
  RANDOM_PITCHES: 'RandomPitches',
} as const;
